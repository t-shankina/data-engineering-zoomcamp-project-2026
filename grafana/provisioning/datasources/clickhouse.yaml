apiVersion: 1

datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    uid: PDEE91DDB90597936
    isDefault: true
    jsonData:
      host: clickhouse
      port: 9000
      username: ${GF_CLICKHOUSE_USER}
      defaultDatabase: ais_db
    secureJsonData:
      password: ${GF_CLICKHOUSE_PASSWORD}
